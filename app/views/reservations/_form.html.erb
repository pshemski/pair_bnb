<%= form_for @reservation, url: confirm_reservations_path do |f| %>
		<%= f.label :Start_date %>
		<%= f.date_field :start_date %>
		<%= f.label :End_date %>
		<%= f.date_field :end_date %>
		<%= f.hidden_field :listing_id, value: params[:listing_id] %>
	<%= f.submit %> 
<% end %>
